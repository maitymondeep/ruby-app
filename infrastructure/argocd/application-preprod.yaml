apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ruby-app-preprod
  namespace: default
spec:
  project: ruby-app
  source:
    repoURL: https://github.com/maitymondeep/ruby-app
    path: infrastructure/helm/environments/preprod
    targetRevision: HEAD
    helm:
      valueFiles:
        - values.yaml
        - environments/preprod/preprod-values.yaml
  destination:
    namespace: preprod
    name: prod
  syncPolicy:
    syncOptions:
      - ApplyOutOfSyncOnly=true
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
